{
  "name": "AutoSorter",
  "author": "Pete",
  "description": "Adds the ability to upgrade all storage types to auto-sorters. Auto-sorters are able to transfer items from other storages on your raft automatically.",
  "version": "1.4.3",
  "license": "MIT",
  "icon": "icon.png",
  "banner": "banner.jpg",
  "gameVersion": "1.1.01",
  "updateUrl": "https://www.raftmodding.com/api/v1/mods/auto-sorter/version.txt",
  "isModPermanent": true,
  "excludedFiles": [],
  "requiredByAllPlayers": true,
  "modSettings": [
    {
      "type": "checkbox",
      "name": "ChangeStorageColorOnUpgrade",
      "text": "change storage color on upgrade",
      "default": true,
      "tooltip": [
        "Determines whether the storage color changes whenever you upgrade a storage to an auto-sorter.",
        "If you disable this, make sure you reload your world, so all storages get back to normal."
      ]
    },
    {
      "type": "slider",
      "name": "MaxSearchResultItems",
      "text": "max. search results",
      "default": 25,
      "range": {
        "type": "number",
        "min": 5,
        "max": 100,
        "decimals": 0
      },
      "tooltip": [
        "The maximum amount of items displayed in the manual mode item filter list.",
        "Recreating those UI elements has a bit of a performance impact, so make sure you lower this value if you are having lags while opening the auto-sorter UI."
      ]
    },
    {
      "type": "slider",
      "name": "ReturnItemsOnDowngradeMultiplier",
      "text": "downgrade items returned",
      "default": 0.5,
      "range": {
        "type": "percent",
        "min": 0,
        "max": 1,
        "decimals": 0
      },
      "tooltip": [
        "The multiplier that is applied when determining how many items the user gets back on a downgrade.",
        "This is a percentage value 100% (you will get all items back), 0% (you won't receive any items back on downgrade)."
      ]
    },
    {
      "type": "slider",
      "name": "CheckIntervalSeconds",
      "text": "storage check interval (seconds)",
      "default": 2,
      "range": {
        "type": "number",
        "min": 1,
        "max": 10,
        "decimals": 0
      },
      "tooltip": [
        "The interval in seconds in which all auto-sorters are checked and transfer items.",
        "If you have a lot of storages on your raft, you might want to increase this value to make sure everything still runs smoothly.",
        "Just keep in mind that it can take up to the set interval for items to be transferred."
      ]
    },
    {
      "type": "checkbox",
      "name": "Debug",
      "default": false
    }
  ]
}